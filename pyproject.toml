# This project was generated with 0.3.18 using template: https://github.com/beeware/briefcase-template@v0.3.18
[tool.briefcase]
project_name = "stempeluhr"
bundle = "com.example"
version = "0.0.1"
url = "https://example.com/stempeluhr"
license = "BSD license"
author = "Semen Zamai"
author_email = "zamay.sv@gmail.com"

[tool.briefcase.app.stempeluhr]
formal_name = "stempeluhr"
description = "stempeluhr application"
long_description = """This simple application helps you track your work time spent
"""
sources = [
    "src/stempeluhr",
]
test_sources = [
    "tests",
]
requires = [
    "arrow==1.3.0",
    "binaryornot==0.4.4",
    "briefcase==0.3.19",
    "build==1.2.1",
    "certifi==2024.7.4",
    "cffi==1.16.0",
    "chardet==5.2.0",
    "charset-normalizer==3.3.2",
    "click==8.1.7",
    "clr-loader==0.2.6",
    "colorama==0.4.6",
    "cookiecutter==2.6.0",
    "gitdb==4.0.11",
    "GitPython==3.1.43",
    "idna==3.7",
    "Jinja2==3.1.4",
    "markdown-it-py==3.0.0",
    "MarkupSafe==2.1.5",
    "mdurl==0.1.2",
    "packaging==24.1",
    "platformdirs==4.2.2",
    "psutil==5.9.8",
    "pycparser==2.22",
    "Pygments==2.18.0",
    "pyproject_hooks==1.1.0",
    "python-dateutil==2.9.0.post0",
    "python-slugify==8.0.4",
    "pythonnet==3.0.3",
    "PyYAML==6.0.1",
    "requests==2.32.3",
    "rich==13.7.1",
    "setuptools==71.1.0",
    "six==1.16.0",
    "smmap==5.0.1",
    "text-unidecode==1.3",
    "toga==0.4.5",
    "toga-core==0.4.5",
    "toga-winforms==0.4.5",
    "tomli_w==1.0.0",
    "travertino==0.3.0",
    "types-python-dateutil==2.9.0.20240316",
    "urllib3==2.2.2",
    "wheel==0.43.0",
]
test_requires = [
    "pytest",
]

[tool.briefcase.app.stempeluhr.macOS]
universal_build = true
requires = [
    "toga-cocoa~=0.4.0",
    "std-nslog~=1.0.0",
]

[tool.briefcase.app.stempeluhr.linux]
requires = [
    "toga-gtk~=0.4.0",
]

[tool.briefcase.app.stempeluhr.linux.system.debian]
system_requires = [
    # Needed to compile pycairo wheel
    "libcairo2-dev",
    # Needed to compile PyGObject wheel
    "libgirepository1.0-dev",
]

system_runtime_requires = [
    # Needed to provide GTK and its GI bindings
    "gir1.2-gtk-3.0",
    "libgirepository-1.0-1",
    # Dependencies that GTK looks for at runtime
    "libcanberra-gtk3-module",
    # Needed to provide WebKit2 at runtime
    # Note: Debian 11 and Ubuntu 20.04 require gir1.2-webkit2-4.0 instead
    # "gir1.2-webkit2-4.1",
]

[tool.briefcase.app.stempeluhr.linux.system.rhel]
system_requires = [
    # Needed to compile pycairo wheel
    "cairo-gobject-devel",
    # Needed to compile PyGObject wheel
    "gobject-introspection-devel",
]

system_runtime_requires = [
    # Needed to support Python bindings to GTK
    "gobject-introspection",
    # Needed to provide GTK
    "gtk3",
    # Dependencies that GTK looks for at runtime
    "libcanberra-gtk3",
    # Needed to provide WebKit2 at runtime
    # "webkit2gtk3",
]

[tool.briefcase.app.stempeluhr.linux.system.suse]
system_requires = [
    # Needed to compile pycairo wheel
    "cairo-devel",
    # Needed to compile PyGObject wheel
    "gobject-introspection-devel",
]

system_runtime_requires = [
    # Needed to provide GTK
    "gtk3",
    # Needed to support Python bindings to GTK
    "gobject-introspection", "typelib(Gtk) = 3.0",
    # Dependencies that GTK looks for at runtime
    "libcanberra-gtk3-module",
    # Needed to provide WebKit2 at runtime
    # "libwebkit2gtk3", "typelib(WebKit2)",
]

[tool.briefcase.app.stempeluhr.linux.system.arch]
system_requires = [
    # Needed to compile pycairo wheel
    "cairo",
    # Needed to compile PyGObject wheel
    "gobject-introspection",
    # Runtime dependencies that need to exist so that the
    # Arch package passes final validation.
    # Needed to provide GTK
    "gtk3",
    # Dependencies that GTK looks for at runtime
    "libcanberra",
    # Needed to provide WebKit2
    # "webkit2gtk",
]

system_runtime_requires = [
    # Needed to provide GTK
    "gtk3",
    # Needed to provide PyGObject bindings
    "gobject-introspection-runtime",
    # Dependencies that GTK looks for at runtime
    "libcanberra",
    # Needed to provide WebKit2 at runtime
    # "webkit2gtk",
]

[tool.briefcase.app.stempeluhr.linux.appimage]
manylinux = "manylinux_2_28"

system_requires = [
    # Needed to compile pycairo wheel
    "cairo-gobject-devel",
    # Needed to compile PyGObject wheel
    "gobject-introspection-devel",
    # Needed to provide GTK
    "gtk3-devel",
    # Dependencies that GTK looks for at runtime, that need to be
    # in the build environment to be picked up by linuxdeploy
    "libcanberra-gtk3",
    "PackageKit-gtk3-module",
    "gvfs-client",
]

linuxdeploy_plugins = [
    "DEPLOY_GTK_VERSION=3 gtk",
]

[tool.briefcase.app.stempeluhr.linux.flatpak]
flatpak_runtime = "org.gnome.Platform"
flatpak_runtime_version = "45"
flatpak_sdk = "org.gnome.Sdk"

[tool.briefcase.app.stempeluhr.windows]
requires = [
    "toga-winforms~=0.4.0",
]

# Mobile deployments
[tool.briefcase.app.stempeluhr.iOS]
requires = [
    "toga-iOS~=0.4.0",
    "std-nslog~=1.0.0",
]

[tool.briefcase.app.stempeluhr.android]
requires = [
    "toga-android~=0.4.0",
]

base_theme = "Theme.MaterialComponents.Light.DarkActionBar"

build_gradle_dependencies = [
    "androidx.appcompat:appcompat:1.6.1",
    "com.google.android.material:material:1.11.0",
    # Needed for DetailedList
    "androidx.swiperefreshlayout:swiperefreshlayout:1.1.0",
]

# Web deployments
[tool.briefcase.app.stempeluhr.web]
requires = [
    "toga-web~=0.4.0",
]
style_framework = "Shoelace v2.3"

